prompt for chatgpt:
Generate code in seperate file in python,  html, css, Javascript,  for the below description of project.
1. Front end is html, css
2. Backend are python,  javascript, flask and notepad.
3. Html page has index.html, data.html, login.html and dashboard.html.
Index.html is a parent file with 3 menu button, login, logout, and view data. 
4. From index.html click login button, it is loaded where username and password  stored in a users.txt file is validated with login, if match open dashboard.html while popup error message. 
5. Dashboard.html display temperature data of cpu and power consumption in the textfield. The live data is display on a graph viewer and refresh data every one second. Its has a save button when click Store data to data.txt file. At the sametime, it is also saving every 1 second into data.txt automatically. 
6. Python code reads temperature and power consumption and return to dashboard for display and data is saving along with date and time into data.txt
7. The index file data button is click, data is display on data.html and loaded on index.html. in a table format.
8. Javascript.js and graph.js is used for all inteface with html and python,  graph.js is for display of temperature value against time and power consumption value against time.
9. Style.css is design to display data on html with 3D look and elegant web page design.
10. All button with 3D look, charts display with 3D look, and table in 3D look.
11. App.py keep only flask code for html. Readsensor.py write code in python to collect temperature and power consumption of cpu. All python code function keep within a class. And create file like users.txt having admin:admin122 and data.txt file should create at the Initialization stage of application. 
Share the folder directory structure and content of the file as ready to use.
